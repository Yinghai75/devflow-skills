# Agent 指引

- 本仓库发布 DevFlow 的 `df-*` Codex skills。
- 与用户沟通、仓库文档和注释默认使用简体中文。
- 修改 skill 时保持单个 skill 的 `SKILL.md` 自包含，避免依赖未发布的本地上下文。
- 新增或调整 skill 后，同步更新根目录 `README.md` 与 `README.en.md` 的总览表。
- 不要提交临时生成文件、API key、会话日志或本地工作区路径中的敏感信息。

